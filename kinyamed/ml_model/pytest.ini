[pytest]
testpaths = tests
# Keep the local tests/ package from being shadowed by an installed one.
addopts = -p no:cacheprovider --import-mode=importlib
filterwarnings =
    ignore::DeprecationWarning
