% ABSTRACT — prose to be written.
%
% NOTHING IN THIS FILE MAY CONTAIN A TYPED NUMBER. Quote the generated macros
% instead; paper/check_tex.py fails the build if a literal appears in prose.
% Available macros are listed in main.tex under \RequireGenerated; the full set
% is in generated/results_macros.tex.
%
% Three corrections established against the corpus on 2026-09-08, so the
% abstract does not repeat descriptions that were checked and found wrong:
%
%   ROWS      330,000  (verified)
%   PHRASES   165 distinct  (verified)
%   CONCEPTS  96, NOT 111. 111 is the count of concepts not ruled out;
%             15 of those have no authored phrase, so they generate nothing.
%   SOURCE    82 of 165 phrases (49.7%) are source=speaker. 109 (66.1%) are
%             speaker or speaker_derived. "165 speaker-authored phrases"
%             overstates it by half, and standing rules 5 and 8 require the
%             machine and speaker categories to stay distinguishable because
%             the paper reports them separately.
%
% The evaluation base belongs here too: the reporting set is 9 distinct
% phrases in 5 phrase groups, of which 4 CRITICAL sentences, 4 URGENT and 1
% ROUTINE. See \input{generated/limitations} for the derived statement.

\begin{abstract}
% TODO
\end{abstract}
