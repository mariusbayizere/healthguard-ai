% LIMITATIONS — hand-written prose PLUS the generated statement.
%
% THE MEASUREMENT LIMITATION IS GENERATED, NOT WRITTEN. The distinct-sentence
% counts and the recall granularity they imply are recomputed from the frozen
% split every time the emitter runs, so they cannot drift from the data the
% way a typed sentence would. Do not restate those numbers here in prose --
% \input the generated fragment and write around it.
%
% Limitations this project has recorded elsewhere and that belong in prose
% here, none of which the emitter can produce:
%
%   - The corpus is Kinyarwanda only. English, French and Swahili arms exist
%     as briefs, not as authored corpora.
%   - Half the phrases are not directly speaker-authored (see abstract.tex).
%   - G1's 0.95 threshold is inherited and its source is unverified.
%   - G3's margin above the degenerate ceiling is a clinical judgement that
%     has not been made.
%   - Eight of the 165 corpus phrases carry an open hold or needs_clinician
%     flag.

\input{generated/limitations}

% TODO: prose limitations beyond the measurement base.
