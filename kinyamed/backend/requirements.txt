# Runtime dependencies for the KinyaMed backend.
# Install with:  pip install -r requirements.txt
#
# ML training dependencies (torch, transformers, datasets, ...) belong to
# kinyamed/ml_model and are intentionally not installed into the API image.

# --- Web framework ---
fastapi==0.136.1
uvicorn[standard]==0.47.0
python-multipart==0.0.20

# --- Authentication ---
PyJWT==2.10.1
bcrypt==4.3.0

# --- Observability ---
structlog==25.4.0

# --- Data validation / settings ---
pydantic==2.13.4
pydantic-settings==2.14.1
email-validator==2.3.0
python-dotenv==1.2.2

# --- Database ---
SQLAlchemy==2.0.49
alembic==1.18.4
psycopg2-binary==2.9.12

# --- Cache / queue ---
redis==7.4.0
